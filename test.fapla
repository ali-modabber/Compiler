%%%this is build my regex,
 and their documentation describes
 the following:%%%

 module F
 input:
 X:real;
 Output:real;
 Begin
  if x>0 then
      Return f(x-1)+f(x-2);
    return 1;
    while
    	code
End
Module main
Begin
    I:Real;
    I=20;
    Write f(i);
                i:real;